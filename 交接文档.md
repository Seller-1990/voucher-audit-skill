# voucher-audit-skill 交接文档

更新时间：2026-04-25
GitHub 仓库：`Seller-1990/voucher-audit-skill`（私有）
说明：项目已初始化 Git，默认分支为 `main`；规则运行版本仍由 `rules/versions/` 与 `active_rules.json` 管理。

---

## 1. 项目背景（前因后果）

本项目目标是把“月末凭证审核”这件事做成一个可脚本化、可被大模型直接调用的 Skill：

1. 从工作目录（workdir）读取两份 Excel 导出：
   - `数据汇总.xlsx`（包含辅助账/序时账、客户映射等）
   - `考核表输出.xlsx`（收入成本表）
2. 按规则执行审核（一致性、阈值波动、格式规范等）。
3. 输出一份结构化的审核报告（xlsx）。
4. 可选：对源 Excel 做“颜色标注 + 右侧回写问题说明列”（Excel COM）。

2026-04-24 之前/当天的主要演进可参考：
1. `README.md`：项目定位、架构、命令说明。
2. `SKILL.md`：Skill 约束（必须 preview；标注二次确认；repair 闭环）。
3. `FIXES_SUMMARY.md`：当日对规则系统/CLI 的修复总结。
4. `TEST_REPORT.md`：当日完整测试报告（以 temp_test 数据为主）。

随后开始进入“真实数据实跑（用户 workdir）”阶段，核心工作从“能跑”转为“跑得准、可回写、好复核、误报低”。

---

## 2. 当前用户实跑场景（本轮任务的真实数据）

用户指定的实跑工作目录：

`D:\分析Work\财务数据分析2026\202603\202603收入成本表20260410.13.59`

该 workdir 下的关键输入（按默认配置）：
1. `数据汇总.xlsx`，期望 sheet：`调整后序时账`
2. `考核表输出.xlsx`，期望 sheet：`收入成本表`

报告输出目录：
`<workdir>\凭证审核输出\凭证审核报告_YYYYMM_*.xlsx`

---

## 3. 项目结构与关键模块

### 3.1 CLI 入口与常用命令

入口：`voucher_audit/__main__.py`，主 CLI：`voucher_audit/cli.py`

常用命令：
```powershell
# 预览（不修改文件）
python -m voucher_audit preview --workdir "D:\...\workdir" --show-inputs

# 检查输入文件/sheet/列（不修改文件）
python -m voucher_audit inspect --workdir "D:\...\workdir"

# 运行审核（生成报告；可选源文件标注）
python -m voucher_audit run --workdir "D:\...\workdir"
python -m voucher_audit run --workdir "D:\...\workdir" --yes --no-annotate

# 只跑指定规则（可多次传入）
python -m voucher_audit run --workdir "D:\...\workdir" --yes --no-annotate --include-rule-id SOME_RULE_ID

# repair：缺文件/缺 sheet/缺列时生成补丁并版本化（需确认）
python -m voucher_audit repair --workdir "D:\...\workdir"
```

### 3.2 规则文件（非常关键）

规则文件分三份（见 `voucher_audit/rules_io.py`）：
1. `rules/app_rules.yaml`：文件名、sheet 匹配、列映射、report_format、annotation_policy 等
2. `rules/audit_rules.yaml`：审核规则 checks（“规则清单”）
3. `rules/compiled_rules.yaml`：运行时把 app_rules + audit_rules 编译合并后的结果

重要行为：
1. 每次执行 `preview/run/inspect` 时会调用 `ensure_compiled_rules()`，用当前 `app_rules.yaml + audit_rules.yaml` 重新写出 `compiled_rules.yaml`。
2. 如果存在 `rules/active_rules.json`，则会改用 active 指针指向的版本；当前仓库里该文件**缺失**，因此默认使用 base rules 并每次重编译。

---

## 4. 源文件回写与标注（Excel COM）现状与坑位

### 4.1 回写列是什么

`voucher_audit/source_annotation.py` 定义了回写列头：

- `凭证审核异常项`
- `凭证审核规则ID`
- `凭证审核命中原因`

### 4.2 “回写看起来为空”的根因（已确认）

用户反馈：“回写没有成功/筛选看到那几列是空白”。

已确认的根因是：回写列通常写在 Excel 表（ListObject/Table）右侧的**表外区域**。如果用户只在“表格内”做筛选，表外列自然显示为空（因为筛选范围不包含表外列），造成“以为没写入”的误判。

用户决策：**只保留表外写入**（不把 Excel Table 自动扩展到包含新列）。

因此后续验证回写时要注意：
1. 不要仅用“表格筛选”的视角判断；需要看整张 sheet 的列区域。
2. 如果一定要表格内筛选，需要另做“扩表”能力，但这与用户当前决策冲突，默认不做。

### 4.3 “没有在错误行进行标记”的根因（未完全修复）

高概率原因：辅助账规则的“高亮列映射”目前很窄。

`voucher_audit/source_annotation.py` 里 `_AUX_RULE_SPECS` 目前配置了：
```python
_AUX_RULE_SPECS = {
  “AUX_HEADCOUNT_DATA_CHECK”: highlight_columns=(“摘要”,)
}
```

这意味着：人次数据检查规则会对”摘要”单元格做颜色高亮；其他辅助账规则即使写了回写列，也可能没有任何高亮。

---

## 5. 已完成的重要业务优化（真实数据向）

### 5.1 收入/成本零值不匹配（限定业务类型）

规则名称：**收入/成本零值不匹配（限定业务类型）**（规则 ID：`INC_REV_COST_ZERO_MISMATCH`）

本轮已实现的关键优化：
1. 对“只有收入没有成本 / 只有成本没有收入”的核对中，涉及按“三级科目（业务类型）”分组时，支持忽略三级科目末尾的百分比后缀（示例：`劳务派遣3%` 归一为 `劳务派遣`）。
2. 该逻辑通过参数开关控制：`biz_type_strip_percent_suffix: true`（已写入 `rules/audit_rules.yaml`）。
3. 已补充单测，避免回归。

业务意义：降低因“三级科目带税率/比例后缀”导致的误报。

---

## 6. 已完成的规则合并（2026-04-24 ~ 2026-04-25）

### 6.1 人次数据检查规则合并

将4条旧辅助账规则合并为1条：

| 旧规则ID | 旧规则名称 | 合并后 |
|----------|-----------|--------|
| AUX_SUMMARY_ZY_PATTERN | ZY码检查 | 已删除 |
| AUX_SUMMARY_YS_PATTERN | YS码检查 | 已删除 |
| AUX_RED_FLUSH_ZS_NONPOSITIVE | 冲销ZS非正数 | 已删除 |
| AUX_SUMMARY_ZS_SUFFIX | ZS码后缀违规 | 已删除 |

**合并后**：`AUX_HEADCOUNT_DATA_CHECK`（人次数据检查），type: `headcount_data_check`

口径确认（2026-04-24 实跑 61 条命中）：
1. ZY/YS 码命中 → 错误
2. ZS 码后缀紧跟文字或 `-` 等非法字符 → 错误（人次数据填写错误）
3. 非冲销场景下 ZS 码数字为负 → 需确认
4. 冲销/红冲场景下 ZS 码数字非负 → 需确认

实现文件：
- `voucher_audit/checks.py`：`_headcount_data_check_aux()` 函数
- `voucher_audit/preview.py`：已添加 `headcount_data_check` 字段映射
- `voucher_audit/source_annotation.py`：`_AUX_RULE_SPECS` 已更新为 `AUX_HEADCOUNT_DATA_CHECK`
- `tests/test_headcount_data_check.py`：12 个测试用例，全部通过

### 6.2 客户归属一致性规则合并

将5条旧收入成本规则合并为1条：

| 旧规则ID | 旧规则名称 | 子检查分类 | 原严重度 |
|----------|-----------|-----------|---------|
| INC_CUSTOMER_MAPPING_CHECK | 客户调整映射不一致 | 实际客户与映射不一致 | 错误 |
| INC_BOOK_CUSTOMER_MULTI_ACTUAL | 账载客户对应多实际客户 | 账载客户对应多实际客户 | 错误 |
| INC_FULL_COMBO_DRIFT | 完整组合主映射漂移 | 客户归属组合漂移 | 错误 |
| INC_ACTUAL_CUSTOMER_MULTI_ENTITY | 实际客户对应多主体 | 实际客户对应多主体 | 需确认 |
| INC_PM_CENTER_CUSTOMER_MULTI_DEPT | 项目管理中心客户多部门 | 项目管理中心客户多部门 | 需确认 |

**合并后**：`INC_CUSTOMER_CONSISTENCY`（客户归属一致性检查），type: `customer_consistency_check`

输出特征：
- 合并后所有子检查命中结果统一输出，增加”问题分类”列区分子检查来源
- 严重度按子检查逻辑分别标记（错误/需确认）
- 报告”主映射异常_友好视图”sheet 已适配新规则ID

实跑结果（2026-04-25，workdir: 202603收入成本表）：

| 子检查分类 | 命中数 | 严重度 |
|-----------|--------|--------|
| 实际客户对应多主体 | 37 | 需确认 |
| 客户归属组合漂移 | 19 | 错误 |
| 项目管理中心客户多部门 | 15 | 需确认 |
| 账载客户对应多实际客户 | 8 | 错误 |
| 实际客户与映射不一致 | 2 | 错误 |
| **合计** | **81** | — |

实现文件：
- `voucher_audit/checks.py`：`_customer_consistency_check_income()` 函数 + `_sub_mapping_check()` 辅助函数
- `voucher_audit/preview.py`：已添加 `customer_consistency_check` 字段映射
- `voucher_audit/source_annotation.py`：`_INCOME_RULE_SPECS` 已更新为 `INC_CUSTOMER_CONSISTENCY`
- `voucher_audit/report.py`：`_build_combo_drift_friendly_view()` 筛选条件已更新
- `rules/audit_rules.yaml`：旧5条已删除，新1条已添加
- `rules/compiled_rules.yaml`：已同步更新

### 6.3 当前生效规则清单

| 规则ID | 规则名称 | 类型 | 范围 |
|--------|---------|------|------|
| AUX_HEADCOUNT_DATA_CHECK | 人次数据检查 | headcount_data_check | aux_ledger |
| INC_CUSTOMER_CONSISTENCY | 客户归属一致性检查 | customer_consistency_check | income_cost |
| INC_REV_COST_ZERO_MISMATCH | 收入/成本零值不匹配（限定业务类型） | rev_cost_zero_mismatch | income_cost |
| INC_NEG_GM_HIGH_RATIO | 负毛利且占比过高 | neg_profit_ratio | income_cost |
| INC_OUTSOURCING_NO_WAGE_OR_HANGKAO | 外包缺工资或挂靠 | outsourcing_missing_cost | income_cost |
| INC_METRIC_PP_CHANGE | 指标同比波动 | metric_pp_change | income_cost |
| INC_VALUE_PP_CHANGE | 返费/挂靠同比波动 | value_pp_change | income_cost |
| INC_RATIO_PP_CHANGE | 返费率/挂靠占比同比波动 | ratio_pp_change | income_cost |

---

## 7. 后续事项（待办清单）

以下是”交接后建议继续推进”的事项，按重要性排序（不强行一次做完）：

### 7.1 P0：规则与实现一致性
1. 确保 `audit_rules.yaml` 的每个 `type` 在 `checks.py/run_checks()` 里都有实现或至少有显式告警（避免静默跳过造成漏检）。
2. 对”合并/替代规则”建立一次性的迁移检查清单，避免只改 YAML 不改实现。

### 7.2 P1：辅助账标注体验
1. 扩充 `_AUX_RULE_SPECS`：为常用辅助账规则配置合理的高亮列（至少：摘要、一级科目、收支项目等）。
2. 明确”表外写入”下的验收方式：不要用 Table 内筛选判断回写；必要时提供一个”扩表开关”，但默认关闭。

### 7.3 P1：单规则实跑能力
1. 目前 `run` 支持 `--include-rule-id`，但 `preview` 不支持过滤；如果用户强烈需要”一条规则一条规则跑”，建议在 `preview` 也支持同样过滤参数（避免预览里仍显示全量规则）。

### 7.4 P1：报告规则名称中文化
1. 当前报告中”概览”sheet 的”执行规则列表”已显示中文名称。
2. `report.py` 的 `_replace_rule_id_with_name()` 已将”规则ID”列替换为中文名称输出。
3. 但”主映射异常_友好视图”sheet 的”规则ID”列仍显示英文 ID（`INC_CUSTOMER_CONSISTENCY`），需确认是否也需要中文化。

### 7.5 P2：规则治理与误报控制
1. 按用户反馈”问题太多”的情况，对规则做分级治理：
   - 错误：确定性强、需要立即处理
   - 需确认：存在合理例外，需要人工复核
   - 需关注：信息提示/风险提示
2. 为每条规则建立”误报案例”记录与回归用样例数据（推荐放在 `tests/fixtures/`，或以最小 DataFrame 的方式单测）。

### 7.6 P3：工程化与可维护性
1. 项目已纳入 Git，并推送到 GitHub 私有仓库。
2. 已增加 GitHub Actions，在 Python 3.10-3.12 上执行 `pytest -q`。
