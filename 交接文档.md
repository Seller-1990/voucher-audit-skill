# voucher-audit-skill 交接文档

更新时间：2026-07-14
GitHub：`Seller-1990/voucher-audit-skill`，私有仓库，默认分支 `main`。

## 项目用途

项目用于月末凭证审核：从工作目录读取 `数据汇总.xlsx` 和 `考核表输出.xlsx`，按 `rules/audit_rules.yaml` 执行 6 条合并规则，生成 Excel 审核报告，并可选通过 Windows Excel COM 标注源工作簿。

## 运行入口

```powershell
pip install -e .
python -m voucher_audit preview --workdir "D:\path\to\workdir" --show-inputs
python -m voucher_audit inspect --workdir "D:\path\to\workdir"
python -m voucher_audit run --workdir "D:\path\to\workdir" --yes --no-annotate
```

安装项目后也可以使用 `voucher-audit` 命令。

## 规则文件

- `rules/app_rules.yaml`：输入文件、Sheet、列映射、报告格式和标注策略。
- `rules/audit_rules.yaml`：审核规则唯一维护入口。
- `rules/compiled_rules.yaml`：运行时合并产物，被 Git 忽略，不手工维护。
- `rules/versions/`、`rules/active_rules.json`：版本化规则和可选激活指针。

当前规则 ID：

1. `INC_CUSTOMER_CONSISTENCY`
2. `INC_REV_COST_ZERO_MISMATCH`
3. `AUX_HEADCOUNT_DATA_CHECK`
4. `INC_NEG_GM_HIGH_RATIO`
5. `INC_OUTSOURCING_NO_WAGE_OR_HANGKAO`
6. `INC_PP_CHANGE`

## 安全约束

- 源文件标注前自动创建备份；写入失败时恢复备份。
- 标注要求 Windows、桌面 Excel 和 `pywin32`，且源工作簿不能被占用。
- `cleanup` 默认不删除；真实清理必须指定 `--workdir` 并传入 `--yes`。
- active 规则路径必须位于仓库内，越界路径会被拒绝。
- 文本上下文只接受有效 UTF-8，不再静默丢弃无法解码的内容。

## 模块边界

- `runner.py`：加载数据、选择月份、运行规则和生成报告。
- `checks.py`：通用检查和规则分派。
- `checks_customer*.py`：客户一致性检查。
- `checks_headcount.py`、`checks_outsourcing.py`、`checks_pp_change.py`：专项规则。
- `checks_legacy_pp.py`：旧同比规则兼容实现。
- `report.py`：报告工作簿总装配。
- `report_customer_consistency.py`、`report_pp_change.py`：客户和同比明细。
- `report_comparison.py`、`report_cost_checks.py`、`report_profit.py`：对比、成本和毛利报告。
- `source_annotation.py`、`excel_annotation_com.py`：标注计划、COM 写入和回滚。
- `rule_guardrails.py`、`rule_patcher.py`：AI 规则补丁审查和版本化应用。

## 验证基线

```powershell
pytest -q
coverage run --source=voucher_audit -m pytest -q
coverage report --fail-under=35
ruff check voucher_audit tools tests
python -m compileall -q voucher_audit tools tests
```

真实 Excel COM 测试为显式启用项：

```powershell
$env:VOUCHER_AUDIT_EXCEL_INTEGRATION = "1"
pytest -q -m excel_com
```

历史设计和实施计划只保留在 `docs/workflow-archive/`，不作为当前运行说明。
